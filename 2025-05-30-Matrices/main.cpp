#include <iostream>
#include <vector>
#include <string>
#include "matrix_utils.h"
#include <iomanip>
#include <fstream>

int main(int argc, char **argv){
  /*const int M = std::stoi(argv[1]);
  const int N = std::stoi(argv[2]);
  std::vector<double> A(M*N, 0.0);
  std::vector<double> v{1, 3, 7.5, -15.7};
  std::vector<double> result;
  //std::vector<double> B (M*N, 0.0);
  fill_matrix(A, M, N);
  fill_matrix(B, M, N);
  std::vector<double> A{0, 1.09, -1, 0};
  std::vector<double> B{3, 1, 1, 0};
  print_matrix(A, M, N);
  matrixPolynomial(A, M, v, result);
  print_matrix(result, M, M);*/

  /*
  id=i*n+j con n columnas y m filas 
  i=id/n
  j=id%n

  elemento a la derecha i*n+j+1
  elemento arriba tiene (1+i)n +j
    */

  return 0;
}